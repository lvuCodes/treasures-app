// The item catalog — shared item vocabulary (shapes, keycaps, colours) that the
// inventory table, the map display, and the recorder all read. The presentational
// item table + steppers are exported here too once extracted.
export {
  ITEM_TYPES,
  KEYCAPS,
  ITEM_COLOR_COUNT,
  FORCED_COLOR,
  itemColor,
  type ItemType,
} from "./item-catalog";
