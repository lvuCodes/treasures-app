// About panel feature. A footer toggle plus a right-hand sidebar describing the
// app's purpose, mechanism, roadmap, and privacy posture. Self-contained: it
// owns its open state and the body-class scoot effect, so the shell renders it
// with no props.
export { About } from "./About";
